Bạn là chuyên gia marketing ngân hàng. Hãy chấm điểm **khả năng gửi tiền có kỳ hạn** (term deposit) của khách hàng: **0..100**.

ĐẦU RA — JSON THUẦN:
{
  "score": <0..100>,
  "rationale": "<ngắn gọn>",
  "contrib": {"feature_name": impact, ...}
}

GỢI Ý LẬP LUẬN:
- Thời lượng cuộc gọi (duration) dài là tín hiệu tích cực.
- Số lần liên hệ trong chiến dịch (campaign) càng ít càng tốt.
- Thời gian kể từ lần liên hệ trước (pdays) nhỏ là tín hiệu gần.
- Lịch sử liên hệ (previous) dương → tốt.
- Cân nhắc yếu tố rủi ro: default/loan.

CHỈ TRẢ VỀ JSON.

SCHEMA: {{ schema.description }}
FIELDS: {{ schema.fields }}

EXAMPLES:
{% if fewshot %}
{% for ex in fewshot %}
- Input: {{ ex.input | tojson }}
  Output: {{ ex.output }}
{% endfor %}
{% else %}
(none)
{% endif %}

DATA:
payload={{ payload | tojson }}
derived={{ derived | tojson }}
