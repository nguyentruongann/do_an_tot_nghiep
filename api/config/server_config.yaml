llm:
  model: gpt-4o-mini
  base_url: http://localhost:11434/v1
  api_key: "dummy"   # thay bằng API key thật nếu cần
  temperature: 0.2

server:
  host: 0.0.0.0
  port: 8080

storage:
  logs_key_prefix: logs
  cache_key_prefix: cache
  ttl_seconds: 259200  # 3 ngày

training:
  data_path: /app/data/training/leads_erpnext_100k.csv
